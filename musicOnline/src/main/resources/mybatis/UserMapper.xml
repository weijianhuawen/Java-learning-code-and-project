<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.musicdemo.mapper.UserMapper">
    <select id="login" resultType="com.example.musicdemo.model.UserLogin">
        select * from userlogin where username=#{username} and password=#{password};
    </select>
    <select id="getUserByName" resultType="com.example.musicdemo.model.UserLogin">
        select * from userlogin where username=#{username};
    </select>

</mapper>